// 단계 3·4 확장 문제. 각 정의의 설명·접근 순서는 해당 쿼리의 학습 목표다.
const problems=[];
const counts={3:14,4:14};
function add(pack,level,title,topic,tables,columns,description,solution,explanation,expectedRows){
 const p={pack,id:`level${level}_${++counts[level]}`,level,title,topic,tables:tables.split(','),columns:columns.split(','),ordered:true,description:description+` 결과 열은 ${columns} 순서다. 별도 기간 조건이 없으면 전체 기간이다.`,solution,explanation,hints:[`핵심 개념: ${topic}. 결과 행의 단위와 연결 키를 확인하세요.`,`접근 순서: ${explanation}`,`-- 빈칸에 출력 식과 나머지 조건을 채우세요.\n${solution.replace(/SELECT [\s\S]*? FROM /,'SELECT /* 출력 식 */ FROM ').replace(/ORDER BY[^)]*$/,'ORDER BY /* 설명의 정렬 기준 */;')}`],starter:'-- 조건과 출력 열을 확인하세요.\nSELECT '};
 if(expectedRows)p.expectedRows=expectedRows;
 problems.push(p);
}
const s=(...a)=>add('shop',3,...a),a=(...a)=>add('academy',3,...a);
s('주문과 고객 이름 연결','INNER JOIN','orders,customers','order_id,name,status','모든 상태의 주문마다 고객 이름을 붙인다. 주문 번호 오름차순.','SELECT o.order_id,c.name,o.status FROM orders o JOIN customers c ON c.customer_id=o.customer_id ORDER BY o.order_id','주문에서 고객 기본키로 연결하면 주문당 한 행을 유지한다.');
s('할인 단가로 결제한 상품','다중 JOIN','products,order_items,orders','product_id,name','구매 당시 단가가 현재 가격보다 작은 결제 행이 있는 상품을 한 번씩 출력한다. 상품 번호 오름차순.','SELECT DISTINCT p.product_id,p.name FROM products p JOIN order_items i ON i.product_id=p.product_id JOIN orders o ON o.order_id=i.order_id WHERE o.status=\'paid\' AND i.unit_price<p.price ORDER BY p.product_id','상품과 구매 행을 연결한 뒤 현재 가격과 과거 단가를 비교하고 상품 중복을 제거한다.',[[3,'컵']]);
s('상품 행 없는 결제 주문','LEFT JOIN 반조인','orders,order_items','order_id','paid 주문 중 상품 행이 전혀 없는 주문을 찾는다. 0원 행은 상품 행으로 인정한다. 주문 번호 오름차순.','SELECT o.order_id FROM orders o LEFT JOIN order_items i ON i.order_id=o.order_id WHERE o.status=\'paid\' AND i.order_id IS NULL ORDER BY o.order_id','외부 조인 후 우측 키가 NULL인 행만 선택한다.',[]);
s('취소 이력만 있는 고객','EXISTS와 NOT EXISTS','customers,orders','customer_id','취소 주문이 하나 이상 있으나 paid 주문은 없는 고객. 주문 없는 고객은 제외한다. 고객 번호 오름차순.','SELECT c.customer_id FROM customers c WHERE EXISTS(SELECT 1 FROM orders o WHERE o.customer_id=c.customer_id AND o.status=\'canceled\') AND NOT EXISTS(SELECT 1 FROM orders o WHERE o.customer_id=c.customer_id AND o.status=\'paid\') ORDER BY c.customer_id','필요한 이력의 존재와 제외할 이력의 부재를 각각 검사한다.',[]);
s('고객별 구매 상품 종류','COUNT DISTINCT와 외부 조인','customers,orders,order_items','customer_id,product_count','모든 고객의 paid 구매 상품 종류 수. 같은 상품의 여러 행과 재구매는 한 종류, 미구매는 0. 고객 번호 오름차순.','SELECT c.customer_id,COUNT(DISTINCT i.product_id) AS product_count FROM customers c LEFT JOIN orders o ON o.customer_id=c.customer_id AND o.status=\'paid\' LEFT JOIN order_items i ON i.order_id=o.order_id GROUP BY c.customer_id ORDER BY c.customer_id','필터를 ON에 두어 미구매 고객을 보존하고 상품 키를 중복 제거한다.');
s('상품별 실제 구매 고객 수','다단계 집계 JOIN','products,order_items,orders','product_id,buyer_count','전체 상품의 paid 구매 고객 수. 고객당 한 번, 0원 구매 포함, 미판매 0. 상품 번호 오름차순.','SELECT p.product_id,COUNT(DISTINCT o.customer_id) AS buyer_count FROM products p LEFT JOIN order_items i ON i.product_id=p.product_id LEFT JOIN orders o ON o.order_id=i.order_id AND o.status=\'paid\' GROUP BY p.product_id ORDER BY p.product_id','결제 조건을 만족하는 주문의 고객 키만 센다.');
s('같은 도시의 고객 쌍','SELF JOIN','customers','customer_a,customer_b','도시가 NULL이 아니고 같은 고객 두 명의 쌍. 작은 번호를 앞에 두고 두 번호 오름차순.','SELECT a.customer_id AS customer_a,b.customer_id AS customer_b FROM customers a JOIN customers b ON a.city=b.city AND a.customer_id<b.customer_id ORDER BY customer_a,customer_b','작은 키 조건으로 자기 자신과 역방향 중복을 제거한다.');
s('같은 주문에서 함께 산 상품 쌍','SELF JOIN과 중복 제거','orders,order_items','product_a,product_b','paid 주문에서 함께 등장한 서로 다른 상품 쌍을 전체 기간 한 번씩 출력한다. 수량과 반복 행은 무시한다. 작은 상품 번호가 앞이며 두 번호 오름차순.','SELECT DISTINCT a.product_id AS product_a,b.product_id AS product_b FROM order_items a JOIN order_items b ON a.order_id=b.order_id AND a.product_id<b.product_id JOIN orders o ON o.order_id=a.order_id WHERE o.status=\'paid\' ORDER BY product_a,product_b','주문 키로 행을 연결하고 상품 키 대소 관계로 쌍의 방향을 고정한다.');
s('본인 가입 전에 발생한 주문','JOIN 날짜 비교','customers,orders','order_id,customer_id','상태와 무관하게 주문일이 고객 가입일보다 이른 주문. 같은 날은 제외한다. 주문 번호 오름차순.','SELECT o.order_id,o.customer_id FROM orders o JOIN customers c ON c.customer_id=o.customer_id WHERE o.ordered_at<c.joined_at ORDER BY o.order_id','다른 테이블의 날짜와 직접 비교한다.');
s('동일 분류 최저가 상품 전체','상관 MIN','products','product_id,category,price','분류별 현재 최저가 상품을 모두 선택한다. 공동 최저 포함, 0원 포함. 상품 번호 오름차순.','SELECT p.product_id,p.category,p.price FROM products p WHERE p.price=(SELECT MIN(q.price) FROM products q WHERE q.category=p.category) ORDER BY p.product_id','분류를 상관 조건으로 고정하고 최소값과 같은 상품을 모두 보존한다.');
s('고객별 마지막 주문의 모든 동시 주문','상관 MAX','orders','customer_id,order_id,ordered_at','각 고객의 모든 상태 주문 중 가장 늦은 날짜에 해당하는 주문을 모두 출력한다. 같은 날 여러 주문 모두 포함. 고객·주문 번호 오름차순.','SELECT o.customer_id,o.order_id,o.ordered_at FROM orders o WHERE o.ordered_at=(SELECT MAX(x.ordered_at) FROM orders x WHERE x.customer_id=o.customer_id) ORDER BY o.customer_id,o.order_id','날짜 최댓값만 비교하므로 마지막 날짜의 동점 주문이 남는다.');
s('다른 주문에서 반복 구매한 상품','집계 서브쿼리','products,orders,order_items','product_id,name','paid 주문 두 건 이상에 등장한 상품. 동일 주문의 반복 상품 행은 한 건이다. 상품 번호 오름차순.','SELECT p.product_id,p.name FROM products p JOIN (SELECT i.product_id FROM order_items i JOIN orders o ON o.order_id=i.order_id WHERE o.status=\'paid\' GROUP BY i.product_id HAVING COUNT(DISTINCT o.order_id)>=2) x ON x.product_id=p.product_id ORDER BY p.product_id','주문 상품 행 수가 아니라 서로 다른 주문 수로 반복 구매를 판정한다.');
s('분류별 미판매 상품 수','반조인 후 집계','products,orders,order_items','category,unsold_count','paid 판매 이력이 없는 상품만 분류별 개수를 센다. 취소만 판매한 상품은 미판매, 0원 paid는 판매다. 미판매 없는 분류 제외. 분류 오름차순.','SELECT p.category,COUNT(*) AS unsold_count FROM products p WHERE NOT EXISTS(SELECT 1 FROM order_items i JOIN orders o ON o.order_id=i.order_id WHERE i.product_id=p.product_id AND o.status=\'paid\') GROUP BY p.category ORDER BY p.category','상품 단위 반조인으로 판매 행 중복의 영향을 없앤 뒤 분류별 집계한다.');
s('취소 이후 다시 결제한 주문','날짜 상관 EXISTS','orders','customer_id,order_id','같은 고객의 취소 주문 날짜보다 뒤에 발생한 paid 주문을 출력한다. 같은 날짜 취소는 인정하지 않는다. 앞선 취소가 여러 건이어도 주문 한 번 출력. 고객·주문 번호 오름차순.','SELECT o.customer_id,o.order_id FROM orders o WHERE o.status=\'paid\' AND EXISTS(SELECT 1 FROM orders x WHERE x.customer_id=o.customer_id AND x.ordered_at<o.ordered_at AND x.status=\'canceled\') ORDER BY o.customer_id,o.order_id','고객 일치와 엄격한 날짜 선후 조건을 함께 상관시킨다.');
s('고객별 결제 주문당 평균 상품 종류','집계 후 AVG','customers,orders,order_items','customer_id,avg_types','모든 고객의 paid 주문별 상품 종류 수 평균을 소수 둘째 자리로 출력한다. 빈 주문은 종류 0, paid 주문 없는 고객의 평균은 NULL. 고객 번호 오름차순.','SELECT c.customer_id,ROUND(AVG(x.types),2) AS avg_types FROM customers c LEFT JOIN (SELECT o.customer_id,o.order_id,COUNT(DISTINCT i.product_id) AS types FROM orders o LEFT JOIN order_items i ON i.order_id=o.order_id WHERE o.status=\'paid\' GROUP BY o.customer_id,o.order_id) x ON x.customer_id=c.customer_id GROUP BY c.customer_id ORDER BY c.customer_id','주문 단위 종류 수를 먼저 만든 뒤 고객 단위 평균을 구한다.');
s('현재 가격 기준 주문 금액 차이','JOIN 계산 집계','orders,order_items,products','order_id,price_difference','paid 주문마다 현재 가격으로 계산한 합계에서 실제 합계를 뺀다. 상품 행 없는 주문은 0, 반복 행은 각각 합산한다. 주문 번호 오름차순.','SELECT o.order_id,COALESCE(SUM(i.quantity*(p.price-i.unit_price)),0) AS price_difference FROM orders o LEFT JOIN order_items i ON i.order_id=o.order_id LEFT JOIN products p ON p.product_id=i.product_id WHERE o.status=\'paid\' GROUP BY o.order_id ORDER BY o.order_id','행별 수량에 현재·구매 단가 차이를 곱한 후 주문별 합산한다.');
s('서울에서만 구매한 상품','긍정 및 부정 EXISTS','products,order_items,orders,customers','product_id','서울 고객의 paid 구매가 있고 서울 이외 또는 도시 NULL 고객의 paid 구매는 없는 상품. 상품 번호 오름차순.','SELECT p.product_id FROM products p WHERE EXISTS(SELECT 1 FROM order_items i JOIN orders o ON o.order_id=i.order_id JOIN customers c ON c.customer_id=o.customer_id WHERE i.product_id=p.product_id AND o.status=\'paid\' AND c.city=\'서울\') AND NOT EXISTS(SELECT 1 FROM order_items i JOIN orders o ON o.order_id=i.order_id JOIN customers c ON c.customer_id=o.customer_id WHERE i.product_id=p.product_id AND o.status=\'paid\' AND (c.city<>\'서울\' OR c.city IS NULL)) ORDER BY p.product_id','원하는 고객 집단의 구매 존재와 집단 외 구매 부재를 별도로 확인한다.');
s('주문에 포함된 분류의 다양성','다중 JOIN COUNT DISTINCT','orders,order_items,products','order_id,category_count','모든 상태의 주문별 상품 분류 수를 출력한다. 빈 주문 0, 같은 분류 중복 제외. 주문 번호 오름차순.','SELECT o.order_id,COUNT(DISTINCT p.category) AS category_count FROM orders o LEFT JOIN order_items i ON i.order_id=o.order_id LEFT JOIN products p ON p.product_id=i.product_id GROUP BY o.order_id ORDER BY o.order_id','상품 개수와 분류 개수를 구분하고 주문을 외부 조인의 기준으로 둔다.');
s('주문 없는 도시 동료가 있는 고객','상관 반조인','customers,orders','customer_id','본인을 제외한 같은 비NULL 도시 고객 중 주문이 전혀 없는 사람이 있는 고객. 본인의 구매 여부는 무관하다. 고객 번호 오름차순.','SELECT c.customer_id FROM customers c WHERE EXISTS(SELECT 1 FROM customers x WHERE x.city=c.city AND x.customer_id<>c.customer_id AND NOT EXISTS(SELECT 1 FROM orders o WHERE o.customer_id=x.customer_id)) ORDER BY c.customer_id','도시 동료를 찾는 존재 검사 안에서 해당 동료의 무주문 여부를 확인한다.');
s('상품별 가장 높은 구매 단가','LEFT JOIN 집계','products,order_items,orders','product_id,max_paid_price','모든 상품의 paid 구매 당시 최고 단가. 취소 제외, 0원은 0, 구매 없음은 NULL. 상품 번호 오름차순.','SELECT p.product_id,MAX(CASE WHEN o.order_id IS NOT NULL THEN i.unit_price END) AS max_paid_price FROM products p LEFT JOIN order_items i ON i.product_id=p.product_id LEFT JOIN orders o ON o.order_id=i.order_id AND o.status=\'paid\' GROUP BY p.product_id ORDER BY p.product_id','유효한 결제 주문에 연결된 행만 조건부 최댓값에 포함한다.');
s('상품별 취소 수량과 결제 수량','조건부 JOIN 집계','products,order_items,orders','product_id,paid_quantity,canceled_quantity','모든 상품별 상태에 따른 수량 합계를 각각 출력한다. 반복 행 합산, 해당 상태 없으면 0. 상품 번호 오름차순.','SELECT p.product_id,COALESCE(SUM(CASE WHEN o.status=\'paid\' THEN i.quantity ELSE 0 END),0) AS paid_quantity,COALESCE(SUM(CASE WHEN o.status=\'canceled\' THEN i.quantity ELSE 0 END),0) AS canceled_quantity FROM products p LEFT JOIN order_items i ON i.product_id=p.product_id LEFT JOIN orders o ON o.order_id=i.order_id GROUP BY p.product_id ORDER BY p.product_id','두 상태를 WHERE로 거르지 않고 CASE로 분리하여 같은 결과 행에서 비교한다.');
s('분류 내 더 비싼 상품 개수','상관 COUNT','products','product_id,more_expensive_count','각 상품과 같은 분류에서 현재 가격이 엄격히 높은 상품 수. 동가는 제외하고 없으면 0. 상품 번호 오름차순.','SELECT p.product_id,(SELECT COUNT(*) FROM products x WHERE x.category=p.category AND x.price>p.price) AS more_expensive_count FROM products p ORDER BY p.product_id','상관 스칼라 집계로 원래 상품 행 수를 유지한다.');
s('고객의 전체 주문 대비 결제 비율','외부 집계 비율','customers,orders','customer_id,paid_pct','모든 고객의 전체 주문 중 paid 주문 비율을 백분율 소수 둘째 자리로 출력한다. 상품 행 유무와 무관하며 주문 없음은 NULL. 고객 번호 오름차순.','SELECT c.customer_id,ROUND(100.0*SUM(CASE WHEN o.status=\'paid\' THEN 1 ELSE 0 END)/NULLIF(COUNT(o.order_id),0),2) AS paid_pct FROM customers c LEFT JOIN orders o ON o.customer_id=c.customer_id GROUP BY c.customer_id ORDER BY c.customer_id','주문 키를 분모로 세고 빈 분모는 NULLIF로 처리한다.');
a('등록 명세에 회원과 강좌 붙이기','다중 INNER JOIN','enrollments,members,courses','enrollment_id,name,title','등록 한 건마다 회원 이름과 강좌 제목을 출력한다. 재등록은 별도 행이다. 등록 번호 오름차순.','SELECT e.enrollment_id,m.name,c.title FROM enrollments e JOIN members m ON m.member_id=e.member_id JOIN courses c ON c.course_id=e.course_id ORDER BY e.enrollment_id','등록의 두 외래키를 각각 기본키에 연결한다.');
a('한 번도 등록되지 않은 강좌','LEFT JOIN 반조인','courses,enrollments','course_id,title','등록이 전혀 없는 강좌. 완료 여부는 무관하다. 강좌 번호 오름차순.','SELECT c.course_id,c.title FROM courses c LEFT JOIN enrollments e ON e.course_id=c.course_id WHERE e.enrollment_id IS NULL ORDER BY c.course_id','강좌를 보존한 외부 조인에서 등록 기본키의 NULL을 검사한다.');
a('학습 중 채점을 기다리는 회원','두 관계 EXISTS','members,activity,attempts','member_id','study 기록과 미채점 응시가 모두 있는 회원. 0분 학습 인정, 여러 기록이어도 회원 한 번 출력. 회원 번호 오름차순.','SELECT m.member_id FROM members m WHERE EXISTS(SELECT 1 FROM activity a WHERE a.member_id=m.member_id AND a.kind=\'study\') AND EXISTS(SELECT 1 FROM attempts t WHERE t.member_id=m.member_id AND t.score IS NULL) ORDER BY m.member_id','독립된 두 일대다 관계의 존재를 검사하여 조인 곱집합을 피한다.');
a('강좌별 미채점 응시 수','외부 조인 NULL 구분','courses,attempts','course_id,ungraded_count','모든 강좌의 score가 NULL인 실제 응시 수. 응시 없는 강좌는 0. 강좌 번호 오름차순.','SELECT c.course_id,COUNT(CASE WHEN a.score IS NULL THEN a.attempt_id END) AS ungraded_count FROM courses c LEFT JOIN attempts a ON a.course_id=c.course_id GROUP BY c.course_id ORDER BY c.course_id','점수 NULL만 검사하면 외부 조인이 만든 가상 행까지 셀 수 있으므로 실제 응시 키를 센다.');
a('회원별 완료 강좌 종류','LEFT JOIN COUNT DISTINCT','members,enrollments','member_id,completed_courses','모든 회원이 한 번 이상 완료한 서로 다른 강좌 수. 미완료 재등록은 완료 이력을 취소하지 않는다. 완료 없음 0. 회원 번호 오름차순.','SELECT m.member_id,COUNT(DISTINCT e.course_id) AS completed_courses FROM members m LEFT JOIN enrollments e ON e.member_id=m.member_id AND e.completed_at IS NOT NULL GROUP BY m.member_id ORDER BY m.member_id','완료 조건을 ON에 두고 강좌 키만 중복 제거한다.');
a('등록 후 완료까지의 강좌별 평균','JOIN 날짜 집계','courses,enrollments','course_id,avg_days','모든 강좌의 완료된 등록별 소요일 평균을 소수 둘째 자리로 출력한다. 당일 완료 0일, 재등록 각각 포함, 완료 없음 NULL. 강좌 번호 오름차순.','SELECT c.course_id,ROUND(AVG(DATEDIFF(e.completed_at,e.enrolled_at)),2) AS avg_days FROM courses c LEFT JOIN enrollments e ON e.course_id=c.course_id AND e.completed_at IS NOT NULL GROUP BY c.course_id ORDER BY c.course_id','완료된 등록 행에 날짜 차이를 적용한 후 강좌별 평균을 계산한다.');
a('회원 본인 평균보다 높은 채점','상관 AVG','attempts','attempt_id,member_id,score','채점된 응시 중 본인의 전체 강좌 채점 평균을 초과하는 응시. 동점 평균 제외, 0점 포함, NULL 제외. 응시 번호 오름차순.','SELECT a.attempt_id,a.member_id,a.score FROM attempts a WHERE a.score>(SELECT AVG(b.score) FROM attempts b WHERE b.member_id=a.member_id) ORDER BY a.attempt_id','상관 집계의 범위를 회원으로 고정하여 강좌를 넘는 개인 기준선을 만든다.');
a('같은 분야 강좌 쌍','SELF JOIN','courses','course_a,course_b','같은 분야에 속하는 서로 다른 강좌 쌍. 작은 번호가 앞이며 두 번호 오름차순.','SELECT a.course_id AS course_a,b.course_id AS course_b FROM courses a JOIN courses b ON a.track=b.track AND a.course_id<b.course_id ORDER BY course_a,course_b','자기 조인에서 분야 일치와 키 대소 관계를 동시에 사용한다.');
a('완료 이력 뒤 다시 등록한 건','날짜 상관 EXISTS','enrollments','enrollment_id,member_id,course_id','같은 회원·강좌의 다른 등록이 이번 등록일보다 앞선 날짜에 완료된 적 있는 등록. 같은 날 완료는 제외한다. 등록 번호 오름차순.','SELECT e.enrollment_id,e.member_id,e.course_id FROM enrollments e WHERE EXISTS(SELECT 1 FROM enrollments x WHERE x.member_id=e.member_id AND x.course_id=e.course_id AND x.enrollment_id<>e.enrollment_id AND x.completed_at<e.enrolled_at) ORDER BY e.enrollment_id','회원·강좌 복합 조건에 완료일의 엄격한 선행 조건을 붙인다.');
a('응시일 당일 공부한 응시','복합 조건 EXISTS','attempts,activity','attempt_id','응시한 회원에게 응시일과 같은 날짜 study 기록이 있는 응시. 0분도 인정, 여러 학습 행이어도 응시 한 번 출력. 응시 번호 오름차순.','SELECT t.attempt_id FROM attempts t WHERE EXISTS(SELECT 1 FROM activity a WHERE a.member_id=t.member_id AND a.activity_date=t.attempted_at AND a.kind=\'study\') ORDER BY t.attempt_id','조인으로 행을 늘리지 않고 날짜와 회원의 존재 여부만 확인한다.');
a('분야별 채점 평균','JOIN AVG','courses,attempts','track,avg_score','채점된 응시가 있는 분야의 응시별 평균 점수, 소수 둘째 자리. 재응시 각각 포함, 0점 포함, NULL 제외. 분야 오름차순.','SELECT c.track,ROUND(AVG(a.score),2) AS avg_score FROM courses c JOIN attempts a ON a.course_id=c.course_id WHERE a.score IS NOT NULL GROUP BY c.track ORDER BY c.track','강좌 속성을 응시 행에 붙인 뒤 응시 단위 가중치를 유지한 채 평균 낸다.');
a('평균보다 많은 수강 등록을 한 회원','집계 서브쿼리 비교','enrollments','member_id,registration_count','등록이 있는 회원만 대상으로 회원별 등록 건수의 평균을 구하고 그 평균보다 등록이 많은 회원을 출력한다. 재등록 각각 포함. 회원 번호 오름차순.','SELECT member_id,COUNT(*) AS registration_count FROM enrollments GROUP BY member_id HAVING COUNT(*)>(SELECT AVG(x.n) FROM (SELECT COUNT(*) AS n FROM enrollments GROUP BY member_id) x) ORDER BY member_id','집계값의 평균은 회원별 건수를 파생 테이블로 만든 뒤 계산한다.');
a('회원별 등록과 응시 건수 나란히','독립 선집계 JOIN','members,enrollments,attempts','member_id,enrollment_count,attempt_count','모든 회원의 등록 건수와 응시 건수를 출력한다. 재등록·재응시·미채점 모두 각각 포함, 없으면 0. 회원 번호 오름차순.','SELECT m.member_id,COALESCE(e.n,0) AS enrollment_count,COALESCE(a.n,0) AS attempt_count FROM members m LEFT JOIN (SELECT member_id,COUNT(*) AS n FROM enrollments GROUP BY member_id) e ON e.member_id=m.member_id LEFT JOIN (SELECT member_id,COUNT(*) AS n FROM attempts GROUP BY member_id) a ON a.member_id=m.member_id ORDER BY m.member_id','서로 다른 일대다 관계를 먼저 집계하여 곱집합으로 건수가 부풀지 않게 한다.');
a('필수 강좌만 등록한 회원','집합 배제','members,enrollments,courses','member_id','적어도 한 강좌 등록이 있으며 선택 강좌 등록은 없는 회원. 재등록 무관, 완료 여부 무관. 회원 번호 오름차순.','SELECT m.member_id FROM members m WHERE EXISTS(SELECT 1 FROM enrollments e WHERE e.member_id=m.member_id) AND NOT EXISTS(SELECT 1 FROM enrollments e JOIN courses c ON c.course_id=e.course_id WHERE e.member_id=m.member_id AND c.required=0) ORDER BY m.member_id','공집합을 제외한 후 선택 강좌의 존재를 부정한다.');
a('같은 지역 회원의 가입 선후 쌍','비등가 SELF JOIN','members','earlier_member,later_member','같은 비NULL 지역에서 가입일이 엄격히 빠른 회원과 늦은 회원 쌍. 동일 가입일 제외. 빠른 회원 번호, 늦은 회원 번호 오름차순.','SELECT a.member_id AS earlier_member,b.member_id AS later_member FROM members a JOIN members b ON a.region=b.region AND a.joined_at<b.joined_at ORDER BY earlier_member,later_member','키 순서가 아니라 가입일 순서로 쌍의 방향을 정한다.');
a('강좌별 첫 채점일 동점 응시','상관 MIN','attempts','course_id,attempt_id','강좌별 최초 채점 날짜의 모든 채점 응시를 출력한다. NULL 점수 제외, 같은 날 동점 모두 포함. 강좌·응시 번호 오름차순.','SELECT a.course_id,a.attempt_id FROM attempts a WHERE a.score IS NOT NULL AND a.attempted_at=(SELECT MIN(b.attempted_at) FROM attempts b WHERE b.course_id=a.course_id AND b.score IS NOT NULL) ORDER BY a.course_id,a.attempt_id','내부와 외부 모두 채점 조건을 적용하고 최소 날짜와 비교한다.');
a('완료했지만 80점에 못 미친 회원 강좌','EXISTS와 반조인 복합키','enrollments,attempts','member_id,course_id','완료 이력과 채점 이력이 있으나 80점 이상 받은 적 없는 회원·강좌 조합. 재등록 중복 제외, 미채점은 성적으로 인정하지 않음, 0점 포함, 응시·완료 선후 무관. 회원·강좌 번호 오름차순.','SELECT DISTINCT e.member_id,e.course_id FROM enrollments e WHERE e.completed_at IS NOT NULL AND EXISTS(SELECT 1 FROM attempts a WHERE a.member_id=e.member_id AND a.course_id=e.course_id AND a.score IS NOT NULL) AND NOT EXISTS(SELECT 1 FROM attempts a WHERE a.member_id=e.member_id AND a.course_id=e.course_id AND a.score>=80) ORDER BY e.member_id,e.course_id','채점 존재를 먼저 확인한 뒤 기준 이상 점수의 부재를 같은 회원·강좌로 검사한다.');
a('회원별 채점 강좌 커버리지','상관 집계 비율','members,courses,attempts','member_id,coverage_pct','전체 현재 강좌 중 한 번 이상 채점받은 강좌 비율을 모든 회원에 대해 백분율 소수 둘째 자리로 출력한다. 등록 불필요, 미채점 제외, 채점 없음 0, 강좌 자체가 없으면 NULL. 회원 번호 오름차순.','SELECT m.member_id,ROUND(100.0*(SELECT COUNT(DISTINCT a.course_id) FROM attempts a WHERE a.member_id=m.member_id AND a.score IS NOT NULL)/NULLIF((SELECT COUNT(*) FROM courses),0),2) AS coverage_pct FROM members m ORDER BY m.member_id','분자는 회원별 서로 다른 채점 강좌 수, 분모는 전체 강좌 수다.');
a('강좌별 등록자의 총 학습 시간','중복 제거 후 JOIN','courses,enrollments,activity','course_id,study_minutes','각 강좌 등록자들의 전체 study 시간을 합산한다. 학습은 강좌별로 귀속되지 않으므로 등록한 각 강좌에 동일 회원 전체 시간이 반영된다. 재등록으로 중복 가산하지 않는다. 전체 강좌 포함, 학습 없음 0. 강좌 번호 오름차순.','SELECT c.course_id,COALESCE(SUM(a.duration),0) AS study_minutes FROM courses c LEFT JOIN (SELECT DISTINCT course_id,member_id FROM enrollments) e ON e.course_id=c.course_id LEFT JOIN activity a ON a.member_id=e.member_id AND a.kind=\'study\' GROUP BY c.course_id ORDER BY c.course_id','회원·강좌 등록 조합을 먼저 중복 제거하여 재등록이 학습 시간을 증폭하지 않게 한다.');
a('응시 없는 등록의 개수','NOT EXISTS 후 집계','courses,enrollments,attempts','course_id,unattempted_registrations','모든 강좌에서 해당 회원·강좌 응시가 전혀 없는 등록 건수를 출력한다. 재등록 각각 계산, 미채점 응시도 있으면 제외. 없으면 0. 강좌 번호 오름차순.','SELECT c.course_id,COUNT(e.enrollment_id) AS unattempted_registrations FROM courses c LEFT JOIN enrollments e ON e.course_id=c.course_id AND NOT EXISTS(SELECT 1 FROM attempts a WHERE a.member_id=e.member_id AND a.course_id=e.course_id) GROUP BY c.course_id ORDER BY c.course_id','외부 조인의 ON에서 응시 없는 등록만 연결하여 전체 강좌를 유지한다.');
a('마지막 활동일보다 앞선 등록','상관 MAX 날짜 비교','enrollments,activity','enrollment_id','회원의 모든 종류 마지막 활동일보다 앞선 날짜에 등록한 건. 활동 없는 회원은 제외, 같은 날짜 제외. 등록 번호 오름차순.','SELECT e.enrollment_id FROM enrollments e WHERE e.enrolled_at<(SELECT MAX(a.activity_date) FROM activity a WHERE a.member_id=e.member_id) ORDER BY e.enrollment_id','활동 없음의 MAX는 NULL이므로 날짜 비교에서 자동 제외된다.');
a('두 분야에 걸쳐 등록한 회원','JOIN HAVING DISTINCT','members,enrollments,courses','member_id,track_count','서로 다른 분야 두 개 이상에 등록한 회원과 분야 수. 재등록과 같은 분야의 다른 강좌는 중복 제거. 회원 번호 오름차순.','SELECT m.member_id,COUNT(DISTINCT c.track) AS track_count FROM members m JOIN enrollments e ON e.member_id=m.member_id JOIN courses c ON c.course_id=e.course_id GROUP BY m.member_id HAVING COUNT(DISTINCT c.track)>=2 ORDER BY m.member_id','회원별 서로 다른 분야 수에 HAVING을 적용한다.');
a('현재 등록자 중 채점 최고점','등록 집합과 점수 JOIN','courses,enrollments,attempts','course_id,best_score','모든 강좌에서 한 번이라도 등록한 회원의 해당 강좌 최고 점수. 등록일과 응시일 선후는 무관, 미채점 제외, 대상 없음 NULL. 강좌 번호 오름차순.','SELECT c.course_id,MAX(a.score) AS best_score FROM courses c LEFT JOIN attempts a ON a.course_id=c.course_id AND EXISTS(SELECT 1 FROM enrollments e WHERE e.course_id=a.course_id AND e.member_id=a.member_id) GROUP BY c.course_id ORDER BY c.course_id','등록 여부는 EXISTS로 확인하여 재등록 중복 없이 응시 점수를 집계한다.');
const w=(...a)=>add('shop',4,...a),v=(...a)=>add('academy',4,...a);
const amounts="SELECT o.customer_id,o.order_id,o.ordered_at,COALESCE(SUM(i.quantity*i.unit_price),0) AS amount FROM orders o LEFT JOIN order_items i ON i.order_id=o.order_id WHERE o.status='paid' GROUP BY o.customer_id,o.order_id,o.ordered_at";
const totalCustomers="SELECT c.customer_id,COALESCE(SUM(i.quantity*i.unit_price),0) AS revenue FROM customers c LEFT JOIN orders o ON o.customer_id=c.customer_id AND o.status='paid' LEFT JOIN order_items i ON i.order_id=o.order_id GROUP BY c.customer_id";
w('다음 결제 예정 간격 확인','LEAD','orders','customer_id,order_id,next_date','paid 주문마다 다음 결제 날짜를 출력한다. 고객별 날짜·주문 번호 순이며 마지막은 NULL, 같은 날 다음 주문 허용. 고객 번호·주문 날짜·주문 번호 오름차순.','SELECT customer_id,order_id,LEAD(ordered_at) OVER(PARTITION BY customer_id ORDER BY ordered_at,order_id) AS next_date FROM orders WHERE status=\'paid\' ORDER BY customer_id,ordered_at,order_id','LEAD는 현재 행 뒤의 값을 가져오며 없는 뒤 행은 NULL이다.');
w('고객별 첫 주문 금액 대비 차이','FIRST_VALUE','orders,order_items','customer_id,order_id,difference','paid 주문마다 자신의 최초 paid 주문액을 뺀 금액. 빈 주문액 0, 최초는 날짜·번호 순으로 한 건 결정. 고객 번호·날짜·주문 번호 오름차순.',`WITH amounts AS (${amounts}) SELECT customer_id,order_id,amount-FIRST_VALUE(amount) OVER(PARTITION BY customer_id ORDER BY ordered_at,order_id) AS difference FROM amounts ORDER BY customer_id,ordered_at,order_id`,'주문별 합계 후 FIRST_VALUE로 최초 주문의 금액을 각 행에 전달한다.');
w('고객별 최종 주문액 나란히','LAST_VALUE 전체 프레임','orders,order_items','customer_id,order_id,last_amount','각 paid 주문 옆에 고객의 최종 paid 주문액을 붙인다. 빈 주문 0, 날짜·번호 순 마지막 한 건. 고객 번호·날짜·주문 번호 오름차순.',`WITH amounts AS (${amounts}) SELECT customer_id,order_id,LAST_VALUE(amount) OVER(PARTITION BY customer_id ORDER BY ordered_at,order_id ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING) AS last_amount FROM amounts ORDER BY customer_id,ordered_at,order_id`,'LAST_VALUE 기본 프레임의 현재 행 종료를 전체 파티션 종료로 확장한다.');
w('고객 매출 삼분위 그룹','NTILE','customers,orders,order_items','customer_id,revenue,bucket','전체 고객을 paid 매출 내림차순·고객 번호 오름차순으로 세 그룹에 최대한 균등 배분한다. 매출 동점도 나눌 수 있고 미구매 0. 그룹·고객 번호 오름차순.',`WITH totals AS (${totalCustomers}) SELECT customer_id,revenue,NTILE(3) OVER(ORDER BY revenue DESC,customer_id) AS bucket FROM totals ORDER BY bucket,customer_id`,'NTILE은 값의 동점보다 행 수 균등 분배를 우선하며 정렬에 고유키가 필요하다.');
w('상품 가격 상대 순위 비율','PERCENT_RANK','products','product_id,price,relative_pct','전체 상품의 가격 오름차순 상대 순위를 백분율 소수 둘째 자리로 출력한다. 동가 같은 값, 상품 하나면 0. 상품 번호 오름차순.','SELECT product_id,price,ROUND(100*PERCENT_RANK() OVER(ORDER BY price),2) AS relative_pct FROM products ORDER BY product_id','PERCENT_RANK는 순위에서 1을 뺀 값을 행 수에서 1을 뺀 값으로 나눈다.',[[1,1000,20],[2,500,0],[3,3000,60],[4,2000,40],[5,5000,80],[6,5000,80]]);
w('결제 주문액 경쟁 순위','RANK','orders,order_items','order_id,amount,amount_rank','paid 주문액 전체 내림차순 경쟁 순위. 빈 주문 0, 같은 금액 공동 순위이며 다음 순위 건너뜀. 순위·주문 번호 오름차순.',`WITH amounts AS (${amounts}) SELECT order_id,amount,RANK() OVER(ORDER BY amount DESC) AS amount_rank FROM amounts ORDER BY amount_rank,order_id`,'주문별 합계를 먼저 만들고 금액만 윈도 정렬에 넣어 동점을 유지한다.');
w('주문별 고객 전체 결제 건수','COUNT OVER','orders','customer_id,order_id,customer_orders','각 paid 주문에 그 고객의 전체 paid 주문 건수를 붙인다. 상품 행 없어도 주문 인정. 고객·주문 번호 오름차순.','SELECT customer_id,order_id,COUNT(*) OVER(PARTITION BY customer_id) AS customer_orders FROM orders WHERE status=\'paid\' ORDER BY customer_id,order_id','그룹으로 행을 줄이지 않고 파티션 집계값을 각 주문에 붙인다.');
w('직전 주문보다 지출이 늘어난 결제','LAG 후 필터','orders,order_items','customer_id,order_id,amount','날짜·번호 순 직전 paid 주문보다 주문액이 엄격히 증가한 paid 주문만 출력한다. 첫 주문 제외, 빈 주문 0. 고객 번호·날짜·주문 번호 오름차순.',`WITH amounts AS (${amounts}), changes AS (SELECT *,LAG(amount) OVER(PARTITION BY customer_id ORDER BY ordered_at,order_id) AS previous_amount FROM amounts) SELECT customer_id,order_id,amount FROM changes WHERE amount>previous_amount ORDER BY customer_id,ordered_at,order_id`,'윈도 결과를 다음 CTE 바깥 WHERE에서 비교한다.');
w('주문 이전까지 쓴 금액','현재 행 제외 프레임','orders,order_items','customer_id,order_id,prior_spend','각 paid 주문 이전의 고객 누적 결제액. 현재 주문 제외, 첫 주문 0, 빈 주문 0. 날짜·번호 순으로 계산하고 고객 번호·날짜·번호 오름차순.',`WITH amounts AS (${amounts}) SELECT customer_id,order_id,COALESCE(SUM(amount) OVER(PARTITION BY customer_id ORDER BY ordered_at,order_id ROWS BETWEEN UNBOUNDED PRECEDING AND 1 PRECEDING),0) AS prior_spend FROM amounts ORDER BY customer_id,ordered_at,order_id`,'프레임 끝을 1 PRECEDING으로 지정해 현재 행을 제외한다.');
w('현재 주문 이후 남은 결제액','미래 방향 프레임','orders,order_items','customer_id,order_id,future_spend','각 paid 주문 다음부터 고객의 마지막 paid 주문까지 금액 합. 현재 제외, 마지막 0, 빈 주문 0. 날짜·번호 순이며 고객 번호·날짜·번호 오름차순.',`WITH amounts AS (${amounts}) SELECT customer_id,order_id,COALESCE(SUM(amount) OVER(PARTITION BY customer_id ORDER BY ordered_at,order_id ROWS BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING),0) AS future_spend FROM amounts ORDER BY customer_id,ordered_at,order_id`,'FOLLOWING 범위로 앞으로 남은 행만 합산한다.');
w('앞뒤 주문을 포함한 지역 최대 금액','양방향 이동 MAX','orders,order_items','customer_id,order_id,local_max','paid 주문마다 자신과 바로 앞·뒤 주문 총 최대 세 건의 최대 주문액. 고객별 날짜·번호 순, 경계에서는 존재하는 행만 사용, 빈 주문 0. 고객 번호·날짜·번호 오름차순.',`WITH amounts AS (${amounts}) SELECT customer_id,order_id,MAX(amount) OVER(PARTITION BY customer_id ORDER BY ordered_at,order_id ROWS BETWEEN 1 PRECEDING AND 1 FOLLOWING) AS local_max FROM amounts ORDER BY customer_id,ordered_at,order_id`,'양쪽 한 행씩 포함하는 프레임에 최댓값을 적용한다.');
w('일자별 결제 주문 수와 전일자 증감','집계 후 LAG','orders','ordered_at,order_count,change_count','paid 주문이 있는 날짜별 주문 수와 직전 관측 날짜 대비 건수 차이. 달력 전날이 아니며 첫 날짜 차이 NULL. 같은 날 주문 모두 합산. 날짜 오름차순.','WITH daily AS (SELECT ordered_at,COUNT(*) AS order_count FROM orders WHERE status=\'paid\' GROUP BY ordered_at) SELECT ordered_at,order_count,order_count-LAG(order_count) OVER(ORDER BY ordered_at) AS change_count FROM daily ORDER BY ordered_at','날짜를 먼저 한 행으로 집계한 후 관측일 사이 차이를 구한다.');
w('도시 안의 가입 순서','다중 파티션 ROW_NUMBER','customers','customer_id,city,join_number','도시별 가입 날짜·고객 번호 순번. 도시 NULL도 한 파티션, 같은 가입일은 고객 번호 순. 고객 번호 오름차순.','SELECT customer_id,city,ROW_NUMBER() OVER(PARTITION BY city ORDER BY joined_at,customer_id) AS join_number FROM customers ORDER BY customer_id','윈도 순서와 결과 출력 순서를 독립적으로 지정한다.',[[1,'서울',2],[2,'부산',2],[3,null,1],[4,'서울',1],[5,'대전',1],[6,'부산',1]]);
w('분류별 최고 가격과 격차','MAX OVER','products','product_id,category,gap_to_max','각 상품의 분류 최고 현재 가격에서 자기 가격을 뺀 금액. 공동 최고 0, 0원 포함. 상품 번호 오름차순.','SELECT product_id,category,MAX(price) OVER(PARTITION BY category)-price AS gap_to_max FROM products ORDER BY product_id','파티션 최댓값을 상세 상품 행에 유지한다.',[[1,'문구',0],[2,'문구',500],[3,'생활',0],[4,'생활',1000],[5,'도서',0],[6,'도서',0]]);
w('매출 동점 고객의 연속 순위','DENSE_RANK','customers,orders,order_items','customer_id,revenue,revenue_rank','전체 고객의 paid 매출 내림차순 연속 순위. 미구매 0, 동점 공동 순위이며 다음 번호를 건너뛰지 않는다. 순위·고객 번호 오름차순.',`WITH totals AS (${totalCustomers}) SELECT customer_id,revenue,DENSE_RANK() OVER(ORDER BY revenue DESC) AS revenue_rank FROM totals ORDER BY revenue_rank,customer_id`,'전체 고객 합계를 만든 뒤 DENSE_RANK로 동점 뒤 순위의 공백을 없앤다.');
w('고객별 두 번째 결제 한 건','ROW_NUMBER 후 선택','orders','customer_id,order_id,ordered_at','고객별 날짜·번호 순 두 번째 paid 주문을 출력한다. 같은 날 두 주문도 별도 순서, 두 건 미만 고객 제외. 고객 번호 오름차순.','WITH ranked AS (SELECT customer_id,order_id,ordered_at,ROW_NUMBER() OVER(PARTITION BY customer_id ORDER BY ordered_at,order_id) AS rn FROM orders WHERE status=\'paid\') SELECT customer_id,order_id,ordered_at FROM ranked WHERE rn=2 ORDER BY customer_id','ROW_NUMBER를 계산한 파생 결과에서 정확한 위치를 선택한다.');
w('주문 금액의 고객 평균 편차','AVG OVER','orders,order_items','customer_id,order_id,deviation','각 paid 주문액에서 해당 고객의 전체 paid 주문 평균액을 뺀 값을 소수 둘째 자리로 출력한다. 빈 주문 0 포함. 고객·주문 번호 오름차순.',`WITH amounts AS (${amounts}) SELECT customer_id,order_id,ROUND(amount-AVG(amount) OVER(PARTITION BY customer_id),2) AS deviation FROM amounts ORDER BY customer_id,order_id`,'전체 파티션 평균을 개별 주문에서 빼되 반올림은 차이를 구한 뒤 한다.');
w('주문일 기준 누적 결제 건수','RANGE 동점 프레임','orders','order_id,ordered_at,cumulative_count','각 paid 주문에 그 날짜까지의 전체 paid 주문 수를 붙인다. 같은 날짜 주문은 모두 같은 누적 건수. 날짜·번호 오름차순.','SELECT order_id,ordered_at,COUNT(*) OVER(ORDER BY ordered_at RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS cumulative_count FROM orders WHERE status=\'paid\' ORDER BY ordered_at,order_id','RANGE는 정렬 값이 같은 모든 동료 행까지 포함한다.');
w('고객별 최초 결제부터 경과일','FIRST_VALUE 날짜','orders','customer_id,order_id,days_since_first','각 paid 주문 날짜와 고객의 최초 paid 날짜 사이 일수. 첫날 여러 주문은 모두 0. 날짜·번호 순 계산, 고객 번호·날짜·번호 오름차순.','SELECT customer_id,order_id,DATEDIFF(ordered_at,FIRST_VALUE(ordered_at) OVER(PARTITION BY customer_id ORDER BY ordered_at,order_id)) AS days_since_first FROM orders WHERE status=\'paid\' ORDER BY customer_id,ordered_at,order_id','첫 행 날짜를 전달한 뒤 DATEDIFF로 경과일을 계산한다.');
w('주문 금액의 고객 내 비중','파티션 SUM 비율','orders,order_items','customer_id,order_id,order_pct','각 paid 주문이 해당 고객의 전체 paid 금액에서 차지하는 백분율, 소수 둘째 자리. 빈 주문 0, 고객 총액 0이면 NULL. 고객·주문 번호 오름차순.',`WITH amounts AS (${amounts}) SELECT customer_id,order_id,ROUND(100.0*amount/NULLIF(SUM(amount) OVER(PARTITION BY customer_id),0),2) AS order_pct FROM amounts ORDER BY customer_id,order_id`,'고객 파티션 합계를 분모로 사용하고 총액 0을 NULLIF로 처리한다.');
w('가격 바로 위의 서로 다른 가격','중복 제거 후 LEAD','products','category,price,next_price','분류별 중복 제거한 현재 가격마다 다음으로 높은 가격을 출력한다. 가장 높은 가격의 다음은 NULL, 0원 포함. 분류·가격 오름차순.','WITH prices AS (SELECT DISTINCT category,price FROM products) SELECT category,price,LEAD(price) OVER(PARTITION BY category ORDER BY price) AS next_price FROM prices ORDER BY category,price','가격 중복을 먼저 제거하여 다음 상품이 아닌 다음 가격을 찾는다.');
w('최대 주문액을 갱신한 기록','누적 MAX와 선행 프레임','orders,order_items','customer_id,order_id,amount','고객별 날짜·번호 순에서 이전 모든 paid 주문액보다 엄격히 큰 주문을 출력한다. 각 고객 첫 주문은 0원이어도 포함, 같은 최고액 재등장 제외. 고객 번호·날짜·번호 오름차순.',`WITH amounts AS (${amounts}), records AS (SELECT *,MAX(amount) OVER(PARTITION BY customer_id ORDER BY ordered_at,order_id ROWS BETWEEN UNBOUNDED PRECEDING AND 1 PRECEDING) AS previous_max FROM amounts) SELECT customer_id,order_id,amount FROM records WHERE previous_max IS NULL OR amount>previous_max ORDER BY customer_id,ordered_at,order_id`,'현재 행을 제외한 누적 최댓값과 비교해야 기록 갱신을 찾을 수 있다.');
w('주문 상태 전환 기록','LAG 후 상태 비교','orders','customer_id,order_id,status','모든 상태 주문을 고객별 날짜·번호 순으로 보아 직전 주문과 상태가 달라진 주문만 출력한다. 고객 첫 주문 제외. 고객 번호·날짜·번호 오름차순.','WITH compared AS (SELECT customer_id,order_id,ordered_at,status,LAG(status) OVER(PARTITION BY customer_id ORDER BY ordered_at,order_id) AS previous_status FROM orders) SELECT customer_id,order_id,status FROM compared WHERE status<>previous_status ORDER BY customer_id,ordered_at,order_id','상태 문자열의 직전 값을 구한 다음 외부 WHERE로 전환 행을 선택한다.');
const dailyStudy="SELECT member_id,activity_date,SUM(duration) AS minutes FROM activity WHERE kind='study' GROUP BY member_id,activity_date";
v('응시 다음 채점 점수','LEAD와 NULL 사전 제외','attempts','attempt_id,next_score','채점된 응시마다 동일 회원·강좌의 다음 채점 점수. 날짜·응시 번호 순, 마지막 NULL. 0점 포함, 미채점 행은 입력에서 제외. 회원·강좌·날짜·응시 번호 오름차순.','SELECT attempt_id,LEAD(score) OVER(PARTITION BY member_id,course_id ORDER BY attempted_at,attempt_id) AS next_score FROM attempts WHERE score IS NOT NULL ORDER BY member_id,course_id,attempted_at,attempt_id','NULL 점수 행을 먼저 제외하면 LEAD가 바로 다음 채점 행을 참조한다.');
v('회원별 첫 학습 시간과 비교','FIRST_VALUE','activity','activity_id,difference','study 기록마다 최초 study 기록의 시간과 차이. 0분 포함, 날짜·활동 번호 순 첫 행, 회원 번호·날짜·활동 번호 오름차순.','SELECT activity_id,duration-FIRST_VALUE(duration) OVER(PARTITION BY member_id ORDER BY activity_date,activity_id) AS difference FROM activity WHERE kind=\'study\' ORDER BY member_id,activity_date,activity_id','날짜가 같아도 활동 번호로 최초 기록을 결정하고 시간 차이를 계산한다.');
v('최종 응시의 미채점 상태 유지','LAST_VALUE NULL 유지','attempts','attempt_id,last_score','모든 응시마다 해당 회원·강좌의 최종 응시 점수. 최종이 미채점이면 이전 점수로 대체하지 않고 NULL. 날짜·응시 번호 순 마지막, 회원·강좌·날짜·번호 오름차순.','SELECT attempt_id,LAST_VALUE(score) OVER(PARTITION BY member_id,course_id ORDER BY attempted_at,attempt_id ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING) AS last_score FROM attempts ORDER BY member_id,course_id,attempted_at,attempt_id','전체 프레임 LAST_VALUE는 마지막 행의 NULL도 그대로 전달한다.');
v('강좌별 응시 점수 사분위 배정','NTILE','attempts','course_id,attempt_id,score,bucket','채점된 응시를 강좌별 점수 내림차순·응시 번호 오름차순으로 네 그룹에 균등 배정한다. 동점도 분할 가능, 0점 포함, NULL 제외. 강좌·그룹·응시 번호 오름차순.','SELECT course_id,attempt_id,score,NTILE(4) OVER(PARTITION BY course_id ORDER BY score DESC,attempt_id) AS bucket FROM attempts WHERE score IS NOT NULL ORDER BY course_id,bucket,attempt_id','강좌 파티션별 NTILE은 각 그룹 행 수 차이를 최대 한 건으로 만든다.');
v('응시별 강좌 내 상대 순위','PERCENT_RANK','attempts','course_id,attempt_id,relative_pct','채점된 응시의 강좌별 점수 오름차순 상대 순위를 백분율 소수 둘째 자리로 출력한다. 동점 같은 값, 채점 한 건이면 0, NULL 제외. 강좌·응시 번호 오름차순.','SELECT course_id,attempt_id,ROUND(100*PERCENT_RANK() OVER(PARTITION BY course_id ORDER BY score),2) AS relative_pct FROM attempts WHERE score IS NOT NULL ORDER BY course_id,attempt_id','강좌별 행 수를 기준으로 정규화하고 점수 동점을 유지한다.');
v('날짜별 학습 참여 인원 누적 합','집계 후 SUM OVER','activity','activity_date,learners,cumulative_visits','study 있는 날짜마다 중복 없는 학습 회원 수와 그 일별 인원 누적 합. 여러 날 참여한 회원은 날짜마다 다시 세며 0분 포함. 날짜 오름차순.','WITH daily AS (SELECT activity_date,COUNT(DISTINCT member_id) AS learners FROM activity WHERE kind=\'study\' GROUP BY activity_date) SELECT activity_date,learners,SUM(learners) OVER(ORDER BY activity_date ROWS UNBOUNDED PRECEDING) AS cumulative_visits FROM daily ORDER BY activity_date','날짜 내 회원 중복만 제거하며 누적 고유 회원 수와 구분한다.');
v('직전 학습일보다 시간이 줄어든 날','LAG 집계 후 필터','activity','member_id,activity_date,minutes','회원별 study 날짜 합계가 직전 study 관측일보다 작은 날만 출력한다. 달력 전날 아님, 첫 학습일 제외, 0분 포함. 회원·날짜 오름차순.',`WITH daily AS (${dailyStudy}), compared AS (SELECT *,LAG(minutes) OVER(PARTITION BY member_id ORDER BY activity_date) AS previous_minutes FROM daily) SELECT member_id,activity_date,minutes FROM compared WHERE minutes<previous_minutes ORDER BY member_id,activity_date`,'활동 행이 아닌 날짜별 시간으로 집계한 후 직전 관측일과 비교한다.');
v('학습일 전후 세 관측일 평균','양방향 AVG 프레임','activity','member_id,activity_date,centered_avg','회원별 study 날짜 합계의 이전·현재·다음 관측일 평균, 소수 둘째 자리. 없는 날짜를 0으로 채우지 않는다. 경계는 존재하는 관측일만, 0분 포함. 회원·날짜 오름차순.',`WITH daily AS (${dailyStudy}) SELECT member_id,activity_date,ROUND(AVG(minutes) OVER(PARTITION BY member_id ORDER BY activity_date ROWS BETWEEN 1 PRECEDING AND 1 FOLLOWING),2) AS centered_avg FROM daily ORDER BY member_id,activity_date`,'날짜별 집계 후 양방향 세 행 프레임으로 중심 이동 평균을 구한다.');
v('첫 채점 대비 향상 폭','FIRST_VALUE 점수 기준선','attempts','attempt_id,improvement','채점된 응시마다 같은 회원·강좌의 최초 채점 점수를 뺀다. 0점 포함, NULL 제외, 날짜·응시 번호 순 최초. 회원·강좌·날짜·응시 번호 오름차순.','SELECT attempt_id,score-FIRST_VALUE(score) OVER(PARTITION BY member_id,course_id ORDER BY attempted_at,attempt_id) AS improvement FROM attempts WHERE score IS NOT NULL ORDER BY member_id,course_id,attempted_at,attempt_id','직전 점수가 아닌 처음 점수를 고정 기준으로 전달한다.');
v('응시까지의 채점 평균','누적 AVG와 NULL','attempts','attempt_id,running_avg','모든 응시마다 같은 회원·강좌의 현재 행까지 채점 평균을 소수 둘째 자리로 출력한다. NULL 점수는 평균 분모에서 제외하고 아직 채점이 없으면 NULL. 날짜·응시 번호 순. 회원·강좌·날짜·번호 오름차순.','SELECT attempt_id,ROUND(AVG(score) OVER(PARTITION BY member_id,course_id ORDER BY attempted_at,attempt_id ROWS UNBOUNDED PRECEDING),2) AS running_avg FROM attempts ORDER BY member_id,course_id,attempted_at,attempt_id','행은 유지하면서 AVG 자체의 NULL 제외 동작을 이용한다.');
v('강좌별 완료 소요일 순위','RANK 날짜 계산','enrollments','course_id,enrollment_id,completion_rank','완료된 등록을 강좌별 소요일 오름차순 경쟁 순위로 출력한다. 당일 완료 0일, 재등록 각각, 동점 공동 순위 후 건너뜀. 미완료 제외. 강좌·순위·등록 번호 오름차순.','SELECT course_id,enrollment_id,RANK() OVER(PARTITION BY course_id ORDER BY DATEDIFF(completed_at,enrolled_at)) AS completion_rank FROM enrollments WHERE completed_at IS NOT NULL ORDER BY course_id,completion_rank,enrollment_id','완료 날짜 자체가 아니라 등록부터 완료까지 걸린 일수로 순위를 계산한다.');
v('회원별 학습 기록 시간 연속 순위','DENSE_RANK','activity','member_id,activity_id,duration_rank','study 기록의 시간을 회원별 내림차순 연속 순위로 출력한다. 같은 시간 동순위, 다음 번호 건너뛰지 않음, 0분 포함. 회원·순위·활동 번호 오름차순.','SELECT member_id,activity_id,DENSE_RANK() OVER(PARTITION BY member_id ORDER BY duration DESC) AS duration_rank FROM activity WHERE kind=\'study\' ORDER BY member_id,duration_rank,activity_id','회원 단위 파티션에서 시간만 정렬하여 동점 기록을 묶는다.');
v('전체 강좌의 등록 건수 순위','LEFT JOIN 후 RANK','courses,enrollments','course_id,registration_count,registration_rank','전체 강좌의 등록 건수 내림차순 경쟁 순위. 재등록 각각 포함, 미등록 0, 동점 공동 순위 후 건너뜀. 순위·강좌 번호 오름차순.','WITH totals AS (SELECT c.course_id,COUNT(e.enrollment_id) AS registration_count FROM courses c LEFT JOIN enrollments e ON e.course_id=c.course_id GROUP BY c.course_id) SELECT course_id,registration_count,RANK() OVER(ORDER BY registration_count DESC) AS registration_rank FROM totals ORDER BY registration_rank,course_id','등록 없는 강좌까지 집계한 뒤 순위를 부여한다.');
v('학습 기록별 회원 총시간 비중','SUM OVER 비율','activity','member_id,activity_id,time_pct','각 study 기록 시간이 회원 전체 study 시간에서 차지하는 비율을 백분율 소수 둘째 자리로 출력한다. 0분 행 포함, 회원 합계 0이면 NULL. 회원·활동 번호 오름차순.','SELECT member_id,activity_id,ROUND(100.0*duration/NULLIF(SUM(duration) OVER(PARTITION BY member_id),0),2) AS time_pct FROM activity WHERE kind=\'study\' ORDER BY member_id,activity_id','회원별 전체 시간을 파티션 합계로 전달하고 0 분모를 처리한다.');
v('강좌 평균과 최고점 함께 보기','복수 윈도 집계','attempts','attempt_id,course_avg,course_max','채점 응시별로 강좌 전체 채점 평균(소수 둘째 자리)과 최고점을 붙인다. 재응시 각각 평균 반영, 0점 포함, NULL 제외. 응시 번호 오름차순.','SELECT attempt_id,ROUND(AVG(score) OVER(PARTITION BY course_id),2) AS course_avg,MAX(score) OVER(PARTITION BY course_id) AS course_max FROM attempts WHERE score IS NOT NULL ORDER BY attempt_id','같은 파티션 정의를 공유하는 두 집계를 상세 행에 함께 붙인다.');
v('이전 활동 종류와 전환 확인','LAG 범주 값','activity','activity_id,kind,previous_kind','모든 활동에서 회원의 직전 활동 종류를 출력한다. 날짜·활동 번호 순, 첫 기록은 NULL, study 0분 포함. 회원·날짜·활동 번호 오름차순.','SELECT activity_id,kind,LAG(kind) OVER(PARTITION BY member_id ORDER BY activity_date,activity_id) AS previous_kind FROM activity ORDER BY member_id,activity_date,activity_id','LAG는 숫자와 날짜뿐 아니라 상태 문자열도 이전 행에서 가져온다.');
v('미채점까지 포함한 응시 순번','복합 파티션 ROW_NUMBER','attempts','member_id,course_id,attempt_id,attempt_number','모든 응시를 회원·강좌별 날짜·응시 번호 순으로 1부터 번호 매긴다. 미채점과 0점 모두 포함. 회원·강좌·순번 오름차순.','SELECT member_id,course_id,attempt_id,ROW_NUMBER() OVER(PARTITION BY member_id,course_id ORDER BY attempted_at,attempt_id) AS attempt_number FROM attempts ORDER BY member_id,course_id,attempt_number','두 키로 파티션을 나누어 다른 강좌의 응시가 번호에 섞이지 않게 한다.');
v('현재 기록 이후 남은 학습 시간','후행 SUM 프레임','activity','activity_id,remaining_minutes','study 기록마다 같은 회원의 현재 행 뒤 모든 study 시간 합을 출력한다. 날짜·활동 번호 순, 마지막 0, 현재 제외, 0분 포함. 회원·날짜·활동 번호 오름차순.','SELECT activity_id,COALESCE(SUM(duration) OVER(PARTITION BY member_id ORDER BY activity_date,activity_id ROWS BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING),0) AS remaining_minutes FROM activity WHERE kind=\'study\' ORDER BY member_id,activity_date,activity_id','미래 프레임의 시작을 다음 행으로 두고 비어 있는 프레임의 합을 0으로 바꾼다.');
v('이번 등록 전 같은 강좌 등록 수','선행 COUNT 프레임','enrollments','enrollment_id,previous_registrations','각 등록에서 동일 회원·강좌의 앞선 등록 건수를 출력한다. 날짜·등록 번호 순, 첫 등록 0, 완료 여부 무관. 등록 번호 오름차순.','SELECT enrollment_id,COUNT(*) OVER(PARTITION BY member_id,course_id ORDER BY enrolled_at,enrollment_id ROWS BETWEEN UNBOUNDED PRECEDING AND 1 PRECEDING) AS previous_registrations FROM enrollments ORDER BY enrollment_id','COUNT는 빈 프레임에서도 0을 반환하며 현재 등록을 분모에 넣지 않는다.');
v('강좌 내 점수 이하 응시 비율','CUME_DIST','attempts','attempt_id,cumulative_pct','채점 응시마다 같은 강좌에서 자신 이하 점수인 응시 비율을 백분율 소수 둘째 자리로 출력한다. 재응시 각각 포함, 동점 동일, 0점 포함, NULL 제외. 응시 번호 오름차순.','SELECT attempt_id,ROUND(100*CUME_DIST() OVER(PARTITION BY course_id ORDER BY score),2) AS cumulative_pct FROM attempts WHERE score IS NOT NULL ORDER BY attempt_id','CUME_DIST는 동점 묶음의 마지막 위치까지 포함한 누적 비율이다.');
v('회원별 최신 완료 등록 선택','ROW_NUMBER 완료 후 필터','enrollments','member_id,enrollment_id,completed_at','완료 이력이 있는 회원별 최근 완료 등록 한 건. 완료일 내림차순, 같은 날은 등록 번호 큰 건 선택. 미완료 제외. 회원 번호 오름차순.','WITH ranked AS (SELECT member_id,enrollment_id,completed_at,ROW_NUMBER() OVER(PARTITION BY member_id ORDER BY completed_at DESC,enrollment_id DESC) AS rn FROM enrollments WHERE completed_at IS NOT NULL) SELECT member_id,enrollment_id,completed_at FROM ranked WHERE rn=1 ORDER BY member_id','미완료를 순위 계산 전에 제외하고 최종 완료일로 행을 선택한다.');
v('강좌별 두 번째 서로 다른 최고 점수','DENSE_RANK 정확한 순위','attempts','course_id,attempt_id,score','강좌별 두 번째로 높은 서로 다른 채점 점수에 해당하는 응시를 모두 출력한다. 해당 점수 동점 모두, 점수 종류 한 개면 제외, NULL 제외. 강좌·응시 번호 오름차순.','WITH ranked AS (SELECT course_id,attempt_id,score,DENSE_RANK() OVER(PARTITION BY course_id ORDER BY score DESC) AS r FROM attempts WHERE score IS NOT NULL) SELECT course_id,attempt_id,score FROM ranked WHERE r=2 ORDER BY course_id,attempt_id','DENSE_RANK로 서로 다른 점수의 위치를 세어 두 번째 점수 전체를 선택한다.');
v('회원별 하루 최고 학습일 전체','집계 후 RANK 필터','activity','member_id,activity_date,minutes','회원별 study 일일 합계가 최대인 날짜를 모두 출력한다. 동점 날짜 모두, 0분도 학습일, 기록 없는 회원 제외. 회원·날짜 오름차순.',`WITH daily AS (${dailyStudy}), ranked AS (SELECT *,RANK() OVER(PARTITION BY member_id ORDER BY minutes DESC) AS r FROM daily) SELECT member_id,activity_date,minutes FROM ranked WHERE r=1 ORDER BY member_id,activity_date`,'활동 행별 순위가 아닌 날짜 합계의 순위를 구해 최고 학습일을 선택한다.');
// 핵심 조건을 생략한 실행 가능한 오답으로 채점 데이터의 구별력을 확인한다.
problems.find(p=>p.id==='level3_16').wrongSql="SELECT DISTINCT p.product_id,p.name FROM products p JOIN order_items i ON i.product_id=p.product_id JOIN orders o ON o.order_id=i.order_id WHERE o.status='paid' AND i.unit_price<=p.price ORDER BY p.product_id";
problems.find(p=>p.id==='level3_19').wrongSql="SELECT c.customer_id,COUNT(i.product_id) AS product_count FROM customers c LEFT JOIN orders o ON o.customer_id=c.customer_id AND o.status='paid' LEFT JOIN order_items i ON i.order_id=o.order_id GROUP BY c.customer_id ORDER BY c.customer_id";
problems.find(p=>p.id==='level4_19').wrongSql="SELECT product_id,price,ROUND(100*PERCENT_RANK() OVER(ORDER BY price,product_id),2) AS relative_pct FROM products ORDER BY product_id";
problems.find(p=>p.id==='level4_28').wrongSql="SELECT product_id,category,MAX(price) OVER()-price AS gap_to_max FROM products ORDER BY product_id";
module.exports=problems;
if(require.main===module){
 const assert=require('node:assert/strict');
 assert.equal(problems.length,92);
 for(const level of [3,4]){const items=problems.filter(p=>p.level===level);assert.equal(items.length,46);assert.deepEqual(items.map(p=>p.id),Array.from({length:46},(_,i)=>`level${level}_${i+15}`));assert.ok(items.filter(p=>p.expectedRows).length>=3);assert.ok(items.filter(p=>p.wrongSql).length>=2);}
 assert.equal(new Set(problems.map(p=>p.title)).size,92);
 assert.equal(new Set(problems.map(p=>p.solution)).size,92);
 for(const p of problems){assert.equal(p.hints.length,3);assert.ok(p.hints[2].includes('/*'));assert.ok(p.solution.includes('ORDER BY'));const pack=require(`./${p.pack}.json`);assert.ok(p.tables.every(t=>pack.tables.some(x=>x.name===t)));assert.ok(!pack.problems.some(x=>x.id!==p.id && x.solution===p.solution));}
 console.log('단계 3·4 총 92개: 개수·번호·힌트·중복·테이블·독립 기대값 검사 통과');
}
